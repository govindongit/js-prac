let alist=["New","Old","Classic"];
let len = alist.length;
console.log("Array length is:", len);

// Increase the array length to 5
alist.length= 5;
console.log("after increasing the length by 5", alist);

// Decrease the array length to 2
alist.length=2;
console.log("after decreasing the length:", alist);

