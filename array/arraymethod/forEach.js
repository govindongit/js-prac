// The forEach() works as a loop over an array where for every element the function just runs once only.

let a = ["Hello", "Gonag", "Govind"];

a.forEach(function(element){
    console.log(element)
});
